<h1>New Project</h1>

<%# orm_with(model: @project, url: projects_path, method: :post, local: true) %>
<%= form_with(model: @email_hierarchy, url: email_hierarchy_index_path, method: :post,local: true) do |form| %>
  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>
  <div class="field">
    <%= form.label :to %>
    <%= form.text_field :to %>
  </div>
    <div class="field">
    <%= form.label :cc %>
    <%= form.text_field :cc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>


<%= link_to 'Back', email_hierarchy_index_path %>
